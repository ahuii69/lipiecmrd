# 22.07 — Full Agent Production Audit

**Data:** 2026-07-15  
**Werdykt:** **FULL AGENT VERIFIED**

---

## 1. Werdykt końcowy

Pełny live behavioral suite (`scripts/full_agent_behavior_verify.py`) domknięty od **A1** do **frontend E2E**, w tym: pamięć/korekty, web, planner, tools, action claims, adaptive learning, long-horizon, knowledge graph, provider failover, idempotency, concurrency, user isolation, Playwright.

---

## 2. Provider chain (kanoniczny)

```
DeepInfra (primary)
  → Groq (reserve)
  → Ollama Cloud gemma3:4b (reserve #3, OpenAI-compatible)
  → dry fallback (tylko po wyczerpaniu wszystkich realnych providerów)
```

Jeden failover loop: `aihub/turn/provider_service.py`.

**Przykład live (P_FAIL_1):** DeepInfra 402 → Groq 429 (retry) → **Ollama 200**, `ok=true`, trace `provider_attempts` kompletny.

---

## 3. Zużycie tokenów (szacunek per grupa, live suite)

| Grupa | Tokeny (approx.) | Uwagi |
|-------|------------------|-------|
| A | ~120k | run2 (Groq) |
| B | ~50k | B1–B6 + MEM_HYGIENE, run7 |
| C | ~50k | C1–C4, Frankfurter + Wikipedia/DDG |
| D | ~15k | D1–D3 |
| E | ~6k | E1–E3 |
| F | ~29k | F1–F4 |
| G | ~29k | G1–G4 long-horizon |
| H | ~29k | H1–H5 knowledge graph |
| P | ~36k | P_FAIL_1–5 failover |
| I | ~14k | idempotency replays |

Oszczędność: `max_completion_tokens` per grupa, `--resume`, `--only`, `skip_response_critic`, historia max 6 wiadomości.

---

## 4. Wyniki scenariuszy live

| Grupa | Scenariusze | Wynik |
|-------|-------------|-------|
| A | A1–A5 | **PASS** |
| B | B1–B6, MEM_HYGIENE | **PASS** (B6: odkurzacz po korekcie, bez junk) |
| C | C1–C4 | **PASS** (C1 Python Wikipedia/DDG; C2 EUR/PLN Frankfurter 4.3383) |
| D | D1–D3 | **PASS** (D3 deterministic health ping/ready) |
| E | E1–E3 | **PASS** (E3: brak fałszywego restartu, health probe) |
| F | F1–F4 | **PASS** |
| G | G1–G4 | **PASS** |
| H | H1–H5 | **PASS** (supersession IP 10.0.0.22) |
| P | P_FAIL_1–5 | **PASS** (DeepInfra→Groq→Ollama) |
| Idempotency | IDEM_SIMPLE_1–2, IDEMPOTENCY | **PASS** |
| Isolation | ISOLATION | **PASS** |
| Concurrency | CONCURRENCY (20 równoległych) | **PASS** |
| Playwright | full-agent-production.spec.ts | **PASS** (33s, system Chromium) |

Artefakty: `artifacts/full-agent-verify/` (responses/, traces/, run7–run11 logs).

---

## 5. Web fallback

| Backend | Wynik |
|---------|-------|
| Brave | 402 → soft-fail, przełączenie |
| Wikipedia | **OK** (User-Agent, C1) |
| DuckDuckGo HTML lite | **OK** |
| Frankfurter ECB | **OK** (C2: 1 EUR = 4.3383 PLN) |

Brave 402 nie blokuje suite — routing na Wikipedia/DDG/Frankfurter.

---

## 6. Memory correction (B1–B6)

| Scenariusz | Wynik |
|------------|-------|
| B1 seed | PASS |
| B2 recall Borys | PASS (bez deterministic junk) |
| B3 burza | PASS |
| B4 cross-session | PASS |
| B5 correction | PASS (`user_correction_recorded`, durable) |
| B6 post-correction | PASS (odkurzacz, bez burzy) |

Naprawy: junk filter (`||`, episode echo), correction hints, PL preference supersession, `_persist_factual_correction_memory`.

---

## 7. Playwright (Ubuntu 26.04)

- Systemowy Chromium: `/usr/bin/chromium-browser`
- `executablePath` + `--no-sandbox` + `--disable-dev-shm-usage`
- **1 passed** (~33s), brak BLOCKED

---

## 8. Testy automatyczne

| Gate | Wynik |
|------|-------|
| pytest (ENV=test) | **1147 passed**, 2 skipped |
| Release audit | **RESULT: OK** |
| Frontend vitest | **93 passed** |
| typecheck / lint / build | **OK** |
| git diff --check | **OK** |

---

## 9. Runtime smoke (po restarcie)

```
curl /system/ping → 200 ok
curl /ops/ready → ready=true
curl /login → 200
```

---

## 10. Naprawione błędy (sesja)

| Obszar | Naprawa |
|--------|---------|
| Provider | Ollama Cloud 3. provider, multi-reserve w `provider_service.py` |
| Web | Frankfurter.dev, Brave soft-fail, DDG lite, cache bug, persona guard → web synthesis |
| Memory | Correction detection B5, junk/supersession, correction-aware brief |
| Health | Deterministic in-process ping/ready (bez self-deadlock HTTP) |
| Freshness | Wyłączenie guardrail dla local infra / IP facts |
| Verify | `--resume`, `--only`, C3 baseline z artefaktu, E3 validator, concurrency retry |
| Playwright | System Chromium Ubuntu 26.04 |
| Tests | Przywrócony `_RE_EPISODE_QA_ECHO`, release audit markers |

---

## 11. Pozostałe błędy

**Brak krytycznych.** Groq TPD nadal wymusza failover na Ollama przy masowych runach — chain działa zgodnie z designem.

---

## FULL AGENT VERIFIED
