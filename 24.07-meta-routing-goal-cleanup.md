# 24.07 — Meta routing + non-actionable goal cleanup

**Data:** 2026-07-17  
**Werdykt:** **META ROUTING FIX VERIFIED**

---

## 1. Root cause

Live smoke (`Powiedz krótko, kim jesteś i jak działasz.`) był technicznie OK (`ok=true`, Groq), ale behawioralnie/kosztowo zły:

1. **Meta-ask** nie miał twardego lekkiego path — `_build_context` zawsze wołał `retrieve_context` + memory pack, pipeline ładował Memory V2 / identity, selector robił bounded memory retrieval, overlays (experience / sim / learning / WK) dokładały się do promptu → `contextual`, `memory_hits>0`, `prompt_tokens≈5352`.
2. **Śmieciowe goals** (`information_need` / `memory_gap` typu „Uzupełnić brak kontekstu: …”) zostawały `active` i wchodziły do `get_active_goals` → `ACTIVE_GOAL_PRESENT` + `selected_goal`.
3. **Fałszywy claim „OpenAI”** — model `openai/gpt-oss-120b` (prefiks rodziny) był interpretowany jako OpenAI API, mimo `provider=groq`.

---

## 2. Zmiany (pliki)

| Plik | Zmiana |
|------|--------|
| `aihub/strategy_selector.py` | rozszerzone markery meta; `meta_ask_refers_to_prior_conversation`; pure meta → `instant` + skip memory; prior-ref → `direct`; reason codes `META_ASK_*`; `StrategyType` + `direct` |
| `aihub/decision_engine.py` | `direct` → execution `direct`, tools off |
| `aihub/goal_engine.py` | `is_actionable_goal` / `get_actionable_goals` / `classify_goal_cleanup` / `cleanup_non_actionable_goals`; silniejszy skip creation; meta wygrywa nad spurious progress |
| `aihub/turn/mixins/prompt_context.py` | pure meta: zero retrieve/pack; provider identity sanitize w persona guard |
| `aihub/turn/mixins/pipeline.py` | skip Memory V2 / identity; cap `max_completion_tokens` 160–256; honest memory flags |
| `aihub/turn/mixins/decision_pre_exec.py` | tylko actionable goals; meta: skip experience/V2/sim/policy/cognitive/learning/WK; force instant/direct |
| `aihub/turn/mixins/prompt_system.py` | `PRODUCT_IDENTITY_PROMPT`; pure meta: strip heavy prompt layers |
| `aihub/turn/mixins/decision.py` | whitelist tools dla `direct` |
| `aihub/response_persona_guard.py` | `sanitize_false_provider_identity` (bez globalnego replace „OpenAI”) |
| `aihub/turn/_ops_ns.py` | eksport nowych guardów |
| `scripts/cleanup_non_actionable_goals.py` | CLI dry-run / apply |
| `tests/test_meta_routing_goal_cleanup_24.py` | testy A–F |

---

## 3. Meta path — przed / po

| Metryka | Przed | Po (live) |
|---------|-------|-----------|
| `selected_strategy` | `contextual` | `instant` |
| memory lookup | tak | `false` |
| `memory_hits` | 3 | 0 |
| `memory_v2_context_injected` | tak (match≈10) | `false` |
| `selected_goal` | meta junk | `null` |
| `ACTIVE_GOAL_PRESENT` | tak | nie |
| planner / web / tools | eskalacja możliwa | off / 0 |
| `simulation_ran` | tak | `false` |
| `experience_lookup_happened` | tak | `false` |
| tokeny | `prompt_tokens≈5352` | `usage_total_tokens=2900` |
| OpenAI API claim | tak | nie |
| provider / model | groq / `openai/gpt-oss-120b` | groq / `openai/gpt-oss-120b` |

Reason codes (live):  
`META_ASK_MEMORY_SKIPPED`, `META_ASK_LIGHTWEIGHT_PATH`, `META_ASK_GOAL_SKIPPED`, `STRATEGY_RULE_INSTANT`, `META_ASK_HEAVY_STAGES_SKIPPED`.

---

## 4. Goals cleanup

### User `8957f43f-85ad-43f4-8855-fee430f78fe8` (wymagany)

| Faza | scanned | matched | cancelled | skipped |
|------|---------|---------|-----------|---------|
| dry-run #1 | 2 | 1 | — | 1 (`has_progress` — bug, naprawiony) |
| apply #1 | 2 | 1 | 1 | 1 |
| apply po fix filtrze | 1 | 1 | 1 | 0 |
| idempotent | 0 open | 0 | 0 | — |

Zarchiwizowane (cancelled + metadata `cleanup_reason=non_goal_meta_or_smalltalk`, `cleanup_version=24.07.1`):

- `Uzupełnić brak kontekstu: Powiedz krótko, kim jesteś i jak działasz.`
- `Informacja: Powiedz krótko, kim jesteś i jak działasz.`

### User live BFF `screenshot_v3` (`550423f4-…`)

- dry/apply: scanned=5, matched=4 cancelled, skipped=1 (prawdziwy goal).

---

## 5. Live chat (authenticated BFF)

- Login: `screenshot_v3` → `user_id=550423f4-9d86-4678-bdf1-52defa9ef38b`
- Message: `Powiedz krótko, kim jesteś i jak działasz.`
- HTTP 200, `ok=true`
- `turn_id=5a65d863-cede-4c2b-ba98-83d49d0d6ea6`
- Odpowiedź: Mordzix / AI-Hub, model językowy + failover — **bez** OpenAI API
- Final: `provider=groq`, `model=openai/gpt-oss-120b`, `provider_calls=1` (DeepInfra 402 → Groq 200 w attempts)

### Replay (ten sam `idempotency_key`)

- ten sam `turn_id`
- ta sama `response_text`
- `idempotency_hit=true`

---

## 6. Testy / audit / health

| Gate | Wynik |
|------|-------|
| `tests/test_meta_routing_goal_cleanup_24.py` (A–F) | **PASS** |
| Pełny pytest (przed ostatnim slim prompt) | **1157 passed**, 2 skipped |
| Targeted chat/failover + meta po slim | **PASS** |
| `release_audit.py` | **RESULT: OK** |
| `git diff --check` | **OK** |
| `/system/ping`, `/ops/ready` | **ok** |
| systemd backend/frontend | **active** |

---

## 7. Artefakty

- `artifacts/full-agent-verify/meta24_live_turn1.json`
- `artifacts/full-agent-verify/meta24_live_turn2.json`
- `artifacts/full-agent-verify/meta24_live_replay.json`

---

## 8. Commit

`fix: optimize meta routing and clean non-actionable goals`  
(bez push)
