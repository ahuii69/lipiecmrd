# ── AI-Hub — requirements.txt (pinned, production) ──────────────
# Wygenerowano na podstawie analizy importów aktywnego kodu.
# Zależności tranzytywne dochodzą przy pip install (środowisko ~70+ pakietów).
# Opcjonalny lokalny freeze: pip freeze > requirements.lock (nie commitowany w tym repo).

# Core API
python-dotenv==1.0.1
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic==2.8.2
starlette==0.52.1

# Async / HTTP
httpx==0.27.2
requests==2.32.3
anyio==4.12.1
sniffio==1.3.1

# Data / vectors
numpy==2.0.1

# User vault (Fernet)
cryptography==43.0.3

# Vector engine (required — full memory/search runtime)
sentence-transformers==5.2.3
faiss-cpu==1.13.2

# Chat uploads (PDF text, no OCR)
pypdf==5.1.0

# STT self-hosted (faster-whisper + zależności bezpośrednie)
faster-whisper==1.2.1
ctranslate2==4.7.1
onnxruntime==1.23.2
av==17.0.0
tokenizers==0.22.2
huggingface-hub==0.36.0
tqdm==4.67.1

# Multipart (FastAPI upload)
python-multipart==0.0.20

# PostgreSQL (optional — DB_BACKEND=postgres)
psycopg2-binary==2.9.10

# Testing
pytest==9.0.2

# Cockpit E2E (optional): playwright install chromium
playwright==1.50.0
