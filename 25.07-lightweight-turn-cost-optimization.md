# 25.07 — Lightweight turn cost optimization

**Data:** 2026-07-17  
**Bazowy commit:** `4b2de3c` (`24.07-meta-routing-goal-cleanup.md`)  
**Werdykt:** **LIGHTWEIGHT TURN VERIFIED**

---

## 1. Źródło ~2900 tokenów (przed)

Po 24.07 meta-ask miał już skip retrieval (`memory_hits=0`, strategy `instant`), ale request do Groq nadal niósł **pełny** system prompt:

| Warstwa | Stan przed (meta) | Szacunek |
|---------|-------------------|----------|
| base system / PERSONA handbook | włączona | tysiące znaków |
| product identity | włączona | ~760 znaków |
| runtime / execution / web / tools policy | włączona | duża |
| psyche / cognitive / learning / knowledge / policy / corrections | włączona lub częściowa | duża |
| tool schemas | `tools=[]` w payload, ale tool loop `iterations≥1` | 0 schemas, koszt pętli |
| memory / history | 0 hits | niski |
| response critic / variants | możliwe | dodatkowe call |

**Live przed (meta24_turn2):** `usage_total_tokens=2900`, `tool_iterations=1`, ciężkie write-backi.

Root cause: brak kanonicznego budżetu `meta_light` — routing był lekki, **prompt i write-back nie**.

---

## 2. Zmiany

| Plik | Rola |
|------|------|
| `aihub/turn/prompt_budget.py` | profile `meta_light\|casual_light\|contextual\|research\|agentic`, `turn_value_class`, `writeback_policy`, slim prompt, `prompt_budget` trace, filtr trivial memory |
| `aihub/turn/mixins/prompt_system.py` | early return: tylko kontrakt `meta_light` |
| `aihub/turn/mixins/pipeline.py` | wybór budżetu przed promptem; tools/schemas off dla `meta_light`; `tool_iterations=0`; minimal writeback; pola `provider_attempt/success/generation` |
| `aihub/memory_v2_service.py` | skip trivial meta write-back (`attempted=false`) |
| `aihub/strategy_selector.py` | markery prior-ref |
| `scripts/cleanup_trivial_meta_memory.py` | dry-run / apply (suppress+archive) |
| `tests/test_lightweight_turn_cost_25.py` | A–H |

---

## 3. Prompt layers — przed / po (meta)

| | Przed | Po (`meta_light`) |
|--|-------|-------------------|
| system | pełny stack | 679 znaków / ~170 est. tok (`meta_light_contract`) |
| layers_included | full | `["meta_light_contract"]` |
| layers_skipped | — | persona handbook, memory, psyche, cognitive, learning, knowledge, policy, corrections, tools, web, capabilities, critic, execution |
| tool schemas | 0 (lista pusta) ale loop ≥1 | 0 schemas, **loop off** |
| history | 0 | 0 (prior-ref: max 2) |
| **prompt_tokens (live)** | ~2700+ (część z 2900) | **302** |
| **completion** | — | **179** (cap 256) |
| **total** | **2900** | **481** |

Diagnostyka live (`trace.prompt_budget`): hash system, `layer_chars.meta_light=679`, bez pełnej treści promptu.

---

## 4. Tool loop — przed / po

| | Przed | Po |
|--|-------|-----|
| `tools` off | tak | tak |
| schemas w payload | brak / puste | brak |
| `tool_iterations` | **1** | **0** |
| `tool_calls_*` | 0 | 0 |
| tool loop entered | tak (min 1) | nie dla `meta_light` |

Legacy tool turns (testy chat_runtime) nadal wykonują model-returned tool_calls poza `meta_light`.

---

## 5. Write-back — przed / po (trivial meta)

**Przed:** experience, Memory V2, psyche heavy, cognitive, learning, knowledge, reflection, success patterns, …

**Po (`turn_value_class=trivial`, `writeback_policy=minimal`):**

- executed: `transcript`, `provider_metrics`
- skipped: memory_v2, knowledge, learning, reflection, experience, cognitive_calibration, procedural_extraction, strategy_learning, self_model, goal_progress, long_horizon, success_patterns

Feedback meta („jesteś za rozwlekły…”) → `turn_value_class=feedback`, nadal lekki prompt, user-model update dozwolony.

---

## 6. Provider attempts

Ujednolicone pola (kompatybilność `provider_calls` zachowana):

- `provider_attempt_count=2` (DeepInfra fail → Groq)
- `provider_success_count=1`
- `provider_generation_count=1`
- `provider_failover_happened=true`
- `provider_calls=1` (sukcesy generacji / legacy)

---

## 7. Cleanup trivial memory

User `8957f43f-85ad-43f4-8855-fee430f78fe8`:

| | scanned | matched | applied | skipped |
|--|---------|---------|---------|---------|
| dry-run | 24 | 11 | — | 13 |
| apply | 24 | 11 | **11** | 13 |
| idempotent | 24 | 0 | 0 | 24 (`already_cleaned` / not trivial) |

Matched: „Memory-guided response” preference junk. Realne fakty / newsy / goal progress — skipped.

---

## 8. Testy targetowane

`tests/test_lightweight_turn_cost_25.py` A–H: **PASS** (11).

Pełny pytest: **1168 passed**, 2 skipped.

---

## 9. Live BFF

Artefakt: `artifacts/full-agent-verify/meta25_live.json`

| Metryka | Wartość |
|---------|---------|
| HTTP / ok | 200 / true |
| strategy | instant |
| budget_profile | meta_light |
| prompt / completion / total | **302 / 179 / 481** |
| memory_hits / lookup / V2 | 0 / false / false |
| selected_goal | null |
| planner / web / tools / sim / experience | off |
| tool_iterations | 0 |
| turn_value_class / writeback | trivial / minimal |
| provider / model | groq / openai/gpt-oss-120b |
| failover | true (attempt=2, success=1) |
| OpenAI API claim | brak |
| errors | [] |

**Replay:** ten sam `turn_id`, ta sama odpowiedź, `idempotency_hit=true`, `provider_attempt_count=0`.

**Contextual control** („Jak nazywa się mój pies?”): `budget_profile=contextual` (nie meta_light), memory hits > 0.

**Agentic control** („Zaplanuj trzyetapową migrację…”): `budget_profile=agentic`, ok=true — nie ograniczone budżetem meta.

---

## 10. Bramki

| Gate | Wynik |
|------|-------|
| pytest | 1168 passed, 2 skipped |
| release_audit | RESULT: OK |
| git diff --check | OK |
| cockpit npm test | 93 passed |
| typecheck / lint / build | OK |
| Playwright (system Chromium) | **1 passed** (`full-agent-production.spec.ts`, 1.2m) |
| health `/system/ping`, `/ops/ready` | ok |
| systemd backend/frontend | active |

---

## 11. Commit

`perf: add bounded lightweight turn execution`  
(bez push)
