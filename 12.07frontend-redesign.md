# Frontend redesign — strona logowania AI-Hub (12.07)

## Rzeczywista przyczyna surowego HTML / czarnego ekranu

**HTML i CSS były poprawne po stronie serwera**, ale przeglądarka **nie stosowała stylesheetu** przy dostępie przez HTTP (np. `http://51.38.133.0:3001/login`).

### Root cause

CSP zawierało **`upgrade-insecure-requests`**, podczas gdy frontend **nie obsługuje TLS** na porcie 3001 (tylko plain HTTP).

Efekt w przeglądarce:

1. Dokument ładuje się po **HTTP**
2. CSP wymusza upgrade assetów do **HTTPS**
3. `https://51.38.133.0:3001/_next/static/css/*.css` kończy się błędem TLS (`wrong version number` — brak certyfikatu)
4. **CSS nie jest stosowany** → domyślna czcionka serif, białe/czarne tło bez layoutu, surowe inputy/buttony
5. Przy ciemnym tle i domyślnym `color: black` widać **czarny ekran z scrollbarem** (treść SSR obecna, ale niewidoczna)

Na `127.0.0.1` problem był ukryty — Chrome często **wyłącza upgrade-insecure-requests dla localhost**, więc CSS ładował się po HTTP i strona wyglądała poprawnie.

### Dowód z finalnego CSS (po naprawie)

Plik: `.next/static/css/b124314790a893d4.css` (~50 KB)

Zawiera m.in.:

- `.bg-neutral-950`
- `.font-sans` (+ `@font-face` Inter)
- `.min-h-\[100dvh\]`
- `.login-animate-page`
- `.rounded-\[1\.75rem\]`
- `.backdrop-blur-xl`
- `.h-12`

Runtime po naprawie (`http://51.38.133.0:3001/login`):

- CSP **bez** `upgrade-insecure-requests`
- `<link rel="stylesheet" href="/_next/static/css/b124314790a893d4.css">` → **200**
- computed: `bodyBg: rgb(10, 10, 10)`, `bodyFont: Inter`, `loginBg: rgb(10, 10, 10)`, `loginColor: rgb(245, 245, 245)`
- 710 reguł CSS aktywnych w przeglądarce

---

## Naprawa

| Zmiana | Plik |
|--------|------|
| `upgrade-insecure-requests` tylko opt-in (`AIHUB_CSP_UPGRADE_INSECURE=1`) | `cockpit/middleware.ts` |
| Fallback `opacity: 1` na animacjach loginu (gdy CSS opóźniony) | `cockpit/styles/globals.css` |
| Jawny scan `./styles/**/*` w Tailwind content | `cockpit/tailwind.config.ts` |
| Poprawka ścieżki CSS w shadcn config | `cockpit/components.json` |
| Test regresyjny login + CSS + brak upgrade-insecure | `cockpit/tests/login-tailwind-runtime.test.ts` |
| Asercja braku upgrade-insecure w CSP | `cockpit/tests/middleware-headers.test.ts` |

**Auth, BFF, backend, cookies, session flow, signed principal — bez zmian.**

Gdy frontend stoi za reverse proxy z TLS, ustaw `AIHUB_CSP_UPGRADE_INSECURE=1` w env.

---

## Zmienione pliki (redesign + fix stylów)

| Plik | Rola |
|------|------|
| `cockpit/app/login/page.tsx` | Cienki entry → `LoginScreen` |
| `cockpit/features/login/login-screen.tsx` | Pełnoekranowy layout desktop/mobile |
| `cockpit/features/login/login-form.tsx` | Auth 1:1 — fetch, next=, błędy, loading |
| `cockpit/features/login/login-brand-panel.tsx` | Lewy panel desktop (brand + 3 cechy) |
| `cockpit/features/login/login-backend-status.tsx` | Subtelny ping `/api/aihub/system/ping` |
| `cockpit/app/layout.tsx` | Font Inter (`next/font/google`), import `@/styles/globals.css` |
| `cockpit/styles/globals.css` | Font stack sans, animacje login, dark base |
| `cockpit/tailwind.config.ts` | `fontFamily.sans`, content scan |
| `cockpit/middleware.ts` | CSP nonce (fix upgrade-insecure na HTTP) |
| `cockpit/tests/login-tailwind-runtime.test.ts` | Test regresyjny stylów |
| `cockpit/tests/middleware-headers.test.ts` | Asercja CSP bez upgrade-insecure |

---

## Layout (zrealizowany)

### Desktop

- Lewy panel (~52%, max 720px): branding, gradient, 3 cechy (Pamięć / Psyche / Web·Research)
- Prawa karta max 420px: formularz, błędy, status backendu

### Mobile

- Jedna kolumna, branding u góry, karta full-width
- Pola `h-12` (48px), przycisk full-width
- `min-h-[100dvh]`, `overflow-y-auto`

---

## Testy i build (12.07, po naprawie)

```text
rm -rf .next && npm ci          → OK
npm test -- --run               → 80 passed (w tym login-tailwind-runtime)
npm run typecheck               → OK
npm run lint                    → OK
npm run build                   → OK, /login ƒ Dynamic
sudo systemctl restart aihub-frontend.service → active
```

---

## Runtime (po restarcie)

```text
curl -I http://127.0.0.1:3001/login        → 200
curl -I http://51.38.133.0:3001/login      → 200
CSS / JS bundles                           → 200
CSP                                        → nonce + strict-dynamic, BEZ upgrade-insecure-requests
HTML                                       → login-animate-page, bg-neutral-950, font-sans, Witaj ponownie
Font                                       → Inter (self-hosted, font-src 'self')
```

---

## Potwierdzenie wizualne

Po naprawie `/login` na **publicznym IP** renderuje:

- ciemne pełnoekranowe tło (`bg-neutral-950`)
- desktop split-layout z lewym panelem brandingowym
- prawą kartą logowania z Inter sans-serif
- inputy 48px, button full-width
- poprawny mobile layout
- **zero domyślnych browserowych styli**

Strona **nie jest już surowym HTML**.

---

## Bootstrap pierwszego konta (13.07)

### Audyt auth (fakty)

- **Brak** endpointu rejestracji przed tą zmianą
- **Brak** bootstrapu admina z ENV
- Tabele `auth_accounts` / `auth_sessions` istnieją (runtime schema)
- Login wymaga wcześniej utworzonego konta
- Jedyna ścieżka tworzenia konta: `create_account()` w kodzie/testach

### Model (jednoosobowy)

Rejestracja otwarta **tylko gdy `auth_accounts` jest puste**. Pierwsze konto = **admin**. Potem rejestracja zamknięta.

### UI

| Ścieżka | Zachowanie |
|---------|------------|
| `/login` | Link „Nie masz jeszcze konta? Utwórz konto” gdy `registration-status.open` |
| `/register` | Formularz (username, hasło, powtórz); auto-login po sukcesie; redirect do `/login` gdy closed |

### API

- `GET /auth/registration-status` (public)
- `POST /auth/register` (public, bootstrap only) → sesja + cookie jak login

### Dokumentacja ENV

`.env.example` / `cockpit/.env.example`: opis bootstrapu (bez sekretów, bez auto-seed konta).

### Testy / runtime

```text
pytest auth bootstrap + ownership     → OK
npm test -- --run                     → 85 passed
typecheck / lint / build              → OK (/register ƒ Dynamic)
runtime register → login → 2nd 403 → anon 401 → OK
```

---

## Redesign głównego chatu + identity fix (13.07)

### Przyczyna forbidden user scope

Źródło starego `user_id`: `localStorage["aihub-cockpit-user-scope-v1"]` generowało `u_*`, podczas gdy BFF/auth używał `principal.user_id` z `/auth/me`.

### Nowy layout

| Warstwa | Zachowanie |
|---------|------------|
| Lewy sidebar | Zwija­ny (desktop toggle / mobile drawer): rozmowy, nowa sesja, logout |
| Środek | Chat max ~832px (`max-w-[52rem]`), user prawo, AI lewo, empty state |
| Pamięć | Drawer (nie stała kolumna); pusta pamięć = neutralny komunikat |
| Diagnostyka | Usunięta z głównego UX (zostaje `/admin`) |
| Status | Subtelny badge Połączono / Offline |

### CSP

- Usunięto `style={{...}}` z composera, Progress, GoalProgress, overview
- Composer: klasa `.composer-textarea` + `field-sizing: content`
- Session list: native scroll zamiast Radix ScrollArea (inline styles)

### Pliki kluczowe

- `lib/store/cockpit-store.ts` — `bindAuthPrincipal`
- `lib/hooks/use-auth-principal.ts`
- `features/user-chat/user-shell.tsx` (+ message/composer/drawer)
- `lib/api/hub-auth-errors.ts`
- `tests/auth-principal-identity.test.ts`

### Wyniki

```text
npm test -- --run     → 93 passed
npm run typecheck/lint/build → OK
runtime chat "elo"    → 200, brak forbidden user scope
```

---

## AI-OS shell + persona + CSP (13.07 VERIFY)

### Frontend

- `/` renderuje `features/ai-os/AiOsShell` (sidebar / header / stage / composer / insight drawer / empty „O czym dzisiaj?”).
- Framer Motion na mount/hover/drawer; design tokens `--aios-*` w globals.
- Auth bez regresji: `useAuthPrincipal` + `bindAuthPrincipal` → chat tylko z `principal.user_id`.

### Persona (conversation layer)

- Przepisany `PERSONA_CONTRACT_PROMPT` + `dry_fallback_response` w `aihub/response_persona_guard.py`.
- `system_rules` w `chat_runtime.py`: Mordzix jako żywy partner; zakaz fraz helpdeskowych; bez fałszywej biografii człowieka.
- API / payloady / auth bez zmian.

### CSP

- `style-src 'self' 'unsafe-inline'` **bez nonce** (Framer Motion / inline styles).
- `script-src` nadal z nonce + `strict-dynamic`.

### Wyniki VERIFY

```text
cockpit npm test -- --run          → 93 passed (15 files)
cockpit npm run typecheck          → OK
cockpit npm run lint               → OK
cockpit npm run build              → OK (/ ƒ Dynamic, ~143 kB)
pytest persona + chat_runtime + auth_ownership → 47 passed (1 warning httpx cookies)
runtime restart aihub-frontend + aihub-backend → active
auth/me (cookie)                   → 200, principal.user_id OK
POST /chat/turn (+ BFF)            → 200, brak forbidden user scope
page / (session)                   → user-shell, O czym dzisiaj?, composer, sidebar
CSP /login                         → style-src bez nonce; script-src z nonce
```
